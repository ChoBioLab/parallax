name: nf-core-resolvinf
channels:
  - conda-forge
  - bioconda
  - pytorch
  - nvidia
dependencies:
  # Core Python
  - python=3.11
  - pip

  # Visualization
  - matplotlib
  - seaborn

  # Essential utilities
  - pyyaml
  - requests

  # Pip dependencies
  - pip:
      - scvi-tools==1.3.2
      - spatialdata==0.4.0
      - spatialdata-io==0.2.0
      - multiqc==1.15
